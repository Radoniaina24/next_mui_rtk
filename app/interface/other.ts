export default interface Other {
  id: string;
  workDay: string;
  workHour: string;
  monthlyLeave: string;
  accruate: string;
  dayOff: Array<number>;
  coefficient: string;
  isFormule: boolean;
}
